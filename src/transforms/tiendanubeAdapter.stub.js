async function publishProducts() {
  throw new Error(
    "Tiendanube adapter is not configured yet. Add credentials and implement this module in a future step.",
  );
}

module.exports = {
  publishProducts,
};
